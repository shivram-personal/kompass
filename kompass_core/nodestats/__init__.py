"""Read-only node enhancements (SPEC §4.7).

Pods-per-node and summed CPU/memory requests, computed in Python from the
engine's already-cached pod and node data. No new cluster permissions, no new
data source, no engine edits.
"""
